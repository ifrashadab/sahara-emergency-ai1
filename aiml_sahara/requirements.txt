fastapi
uvicorn[standard]
chromadb
sentence-transformers
anthropic
python-dotenv
pydantic

# Kept only if you want to keep the original classifier as an optional
# offline fallback when the LLM API is unreachable — not required for the
# core RAG flow.
# transformers
# torch
